# Copy to .env.local and fill in to ENABLE AUTH (login page + route guard).
# Leave unset and the app runs open — no login required (demo mode).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
